{
  "name": "polymer-starter-kit",
  "version": "1.0.0",
  "description": "Its a solution for service worker update when new release found from firebase, service will register automatically when and real timly while version is changed inside firebase",
  "license": "BSD-3-Clause",
  "devDependencies": {
    "eslint": "^3.19.0",
    "eslint-config-google": "^0.7.0",
    "eslint-plugin-html": "^2.0.0"
  },
  "scripts": {
    "deploy" : "polymer build && firebase deploy",
    "lint": "npm run lint:javascript && polymer lint",
    "lint:javascript": "eslint . --ext js,html --ignore-path .gitignore",
    "test": "polymer test",
    "test:integration": "polymer build # test that psk builds without error with the CLI"
  }
}
